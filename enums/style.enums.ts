export enum ESize { //"tiny" | "small" | "medium" | "large" | "giant"
  Tiny = "tiny",
  Small = "small",
  Medium = "medium",
  Large = "large",
  Giant = "giant",
}

export enum EStatus { // "basic" | "primary" | "success" | "info" | "warning" | "danger" | "control"
  Basic = "basic",
  Primary = "primary",
  Success = "success",
  Info = "info",
  Warning = "warning",
  Danger = "danger",
  Control = "control",
}

export enum EAppearance { // "filled" | "outline" | "ghost"
  Filled = "filled",
  Outline = "outline",
  Ghost = "ghost",
}

export enum EShape { //  "rectangle" | "semi-round" | "round"
  Rectangle = "rectangle",
  SemiRound = "semi-round",
  Round = "round",
}

export enum EFieldSize { // "small" | "medium" | "large"
  Small = "small",
  Medium = "medium",
  Large = "large",
}
